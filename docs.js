// import openDb from './db/database.mjs';

const docs = {
    getAll: async function getAll() {
        // let db = await openDb();
        return "Get all from docs";
    }
};

export default docs;
